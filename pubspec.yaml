name: utfpr_solicitacoes_app
description: App para enviar solicitações e reclamações na UTFPR
publish_to: "none"

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.12.0
  cloud_firestore: ^4.7.0
  firebase_auth: ^4.6.0
  firebase_storage: ^11.2.0
  geolocator: 14.0.2
  image_picker: ^0.8.7+4
  provider: ^6.0.5
  intl: ^0.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1
